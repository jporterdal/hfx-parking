Section 1 stands up the mirror and clears the ground. Section 2 keeps it current. Section 3 proves
it faithful at the row level. Section 4 moves the derivation onto it and proves it faithful at the
figure level. Sections 5 to 7 replace the generated file with the served application. Section 8
reconciles the documentation. Section 9 verifies.

The sequence matters: `src/hotspots.py` keeps working against the live service until section 4, and
the application is not moved onto derived tables until 4.9 has proven the derivation reproduces
every published figure. Rollback before section 5 is deleting the store.

This change absorbs the outstanding work of `add-parking-hotspot-map`, which is archived rather
than continued. Tasks carried over from it are marked **[was N.n]** against their original number.

**Preconditions, not tasks:** a Postgres instance to mirror into, and a place to run the sync.

## 1. Stand up the mirror

- [x] 1.1 Remove `.github/workflows/nightly.yml` first, and verify no scheduled job commits to `out/` while the rest of this change is in progress — left running, the bot churns the very files section 8 is trying to pin down
- [x] 1.2 Fix the mirror's scope before designing for it: service requests, custom fields and census dissemination areas, each **in full**, and verify `311_Call_Details` is not mirrored — at 4,968,536 rows and ~1.2 GB it is three times everything else combined, and `design.md` M10 records why it buys nothing
- [x] 1.3 Store the custom-fields layer key-value as published — 1,156,710 rows across 88 distinct `CUSTOM_FIELD_NAME` values — rather than pivoted on ingest, and verify the stored shape matches the source layer so its count can be checked against the service's own
- [x] 1.4 Derive a per-call view over the seven parking attributes (`Alleged Violation`, `Property Ownership`, `Vehicle Was Towed`, `Vehicle Make`, `Vehicle Model`, `Vehicle Colour`, `Vehicle Province`), and verify it yields one row per request for the 110,900 calls carrying an alleged violation — a pivot cannot be the stored form, because 88 field names do not fit seven columns
- [x] 1.5 Define the schema for requests, custom fields, census areas, sync history, retained list history and triage decisions, and verify each downstream section's reads are supported without a later migration
- [x] 1.6 Stand up a `pytest` harness with a dependency manifest, a `tests/` tree and a runnable entry point, and verify `pytest` passes from a clean checkout — every "verify" in this file is a manual inspection until this exists, and the reconciliations in 3.6 and 4.9 need to be re-runnable rather than performed once **[was 12.4, partial]**
- [x] 1.6a Make the suite runnable with no network access, and verify it passes with outbound requests blocked — fixtures stand in for the source, so a source outage and a code regression stop looking alike
- [x] 1.6b Record that the project now has a dependency manifest where it previously had none, and verify `README.md`'s "No keys and no install" is not left describing the server (see 8.3) — the stdlib-only constraint was a hackathon property of the viewer's experience, and it survives for the viewer only
- [x] 1.7 Load each layer by offset paging to exhaustion, and verify stored row counts match the service's own count-only query per layer — 478,458 requests and 1,156,710 custom-field rows at time of writing
- [x] 1.8 Make the initial load resumable at page granularity, and verify a load interrupted partway continues from the last completed page rather than restarting
- [x] 1.9 **Report observed on-disk size per mirrored layer** in the implementation findings, and verify the figures against the ~300–400 MB total estimate rather than restating the estimate
- [x] 1.10 Measure the full load's wall-clock time and request count — observed 1,640 pages, 1,643 requests, 25m10s, 375.5 MB on disk. The 1,250-page/0.52s prediction was sized against the parking slice only; the full custom-fields layer is 1,157 pages on its own

## 2. Sync by replacing the published version

**This section was implemented against an incremental design and then reversed.** `design.md` M2
records why: the layers are published snapshots, not append logs — `hasStaticData` is true, the
service offers only `Query` and `Extract`, and `ObjectId` is a dense 1-to-N row counter reassigned
at each publish. A watermark cannot be made correct against a source whose identifiers carry no
meaning across versions. `design.md` M2a keeps the question open for later evidence.

Tasks marked **[retired]** were completed and are being removed from scope. They are not failures
and they are not pending; the method they implemented no longer exists. Their tests come out with
them.

- [retired] 2.1 Advance a per-layer `ObjectId` watermark — superseded by reload-on-publish
- [retired] 2.2 Verify monotonicity on the requests layer — the assumption it guarded is gone
- [retired] 2.2a Check monotonicity on custom fields through its request's date — same
- [retired] 2.3 Refetch the open request set — subsumed: a full reload retrieves current state for every row
- [retired] 2.3a Measure both halves of the open-set refetch — measured (requests 3,351 rows / 5.7s; custom fields 1,588 rows / 36.1s), retained in M2 as evidence, mechanism removed
- [retired] 2.4 Verify a tow flag set after filing is reflected — a reload reflects it without a special path
- [x] 2.5 Handle a no-op sync, and verify a sync finding nothing new is recorded as successful with no error raised
- [x] 2.6 Poll each layer's `editingInfo.lastEditDate` nightly and pull only when it advances, and verify a poll against an unchanged source performs no pull and is recorded as successful
- [x] 2.6a Keep the poll cadence and the pull cadence separate, and verify a week in which the source publishes once produces seven polls and one pull
- [x] 2.6b Record the observed source last-edit timestamp with every sync so the cadence accumulates as measurement, and verify the interval history is queryable
- [x] 2.6c Skip re-pulling static reference layers, and verify the census layer is not re-pulled merely because a schedule fired
- [retired] 2.6d Keep the watermark pull and open-set refetch on their own schedule independent of the poll — there is no pull independent of the version clock any more
- [x] 2.6e Verify the sync runs unattended with no credentials
- [x] 2.7 Implement full reload as a supported operation, and verify a reload after an induced divergence restores agreement with the service's counts

New, replacing the retired mechanism:

- [x] 2.8 Make full reload the path taken when a layer's published version advances, and verify a poll finding an advanced `lastEditDate` triggers a reload whose stored count reconciles against the service's own count
- [x] 2.9 Replace a layer's contents atomically so a reload cannot leave the mirror part-old and part-new, and verify a reload interrupted midway leaves the previous version intact and queryable rather than a mixture
- [x] 2.10 Remove the watermark pull, both monotonicity checks, the open-set refetch and the `identifier_reassigned` check together with their tests, and verify nothing reachable still reads `layer_state.watermark` as a sync input
- [x] 2.11 Retain each replaced version's identity before it is overwritten — published edit timestamp, row count, highest identifier, and a sample of `REQUEST_ID` to `ObjectId` mappings large enough to detect renumbering — and verify the retained sample is queryable after the reload that replaced it
- [x] 2.12 Verify renumbering is detectable across two retained versions, by checking whether a given `REQUEST_ID` kept its `ObjectId`, and verify the check reports an answer rather than an error when only one version has been retained
- [x] 2.13 Record in `design.md` M2a when the watermark question should next be examined — at least four observed publishes, roughly a month at the current cadence — and verify the retained data is sufficient to answer it without re-instrumenting

## 3. Make staleness impossible to miss, and prove the mirror faithful at row level

- [x] 3.1 Record every sync attempt with its start, outcome, watermark reached, per-layer rows inserted and updated, observed source last-edit timestamp, and error on failure, and verify a failed sync appears as a row rather than as an absence
- [x] 3.2 Expose last attempt and last success separately, and verify a sync failing repeatedly reports a recent attempt and an unchanged last-success time
- [x] 3.3 Expose four clocks separately — source last-edit, most recent call date, last successful sync, next sync due — and verify each is distinguishable from the others, including the case where syncs succeed while the source stops advancing
- [x] 3.4 Report whether the mirror is behind the source, and verify the case where HRM has published more recently than the last successful sync is distinguished from the case where HRM simply has not published
- [x] 3.5 Compare per-layer stored counts against the service's count query on every sync, and verify a divergence is reported with both figures rather than silently resolved
- [x] 3.6 **Reconcile at row level before anything depends on the mirror.** Verify the mirror's `Driveway` call set and its custom-field rows are identical to the service's for the same selection — same request ids, same field values, same counts. This is provable with the mirror alone; figure-level reconciliation is 4.9, because it needs a derivation to exist

## 4. Move the derivation onto the mirror

- [x] 4.1 Read selection, joining, outcome and vehicle attachment, address reduction, doorway location and census placement from the mirror, and verify no network request is issued during a derivation
- [x] 4.4 Move retry and paging out of the derivation into the mirror, and verify the census layer is read locally like every other layer, closing the retry asymmetry recorded as D14a — done with 4.1, in the new mirror derivation rather than in `src/hotspots.py`, which stays stdlib-only and live as the baseline 4.9 reconciles against **[was 12.1]**
- [x] 4.2 Remove the live-fallback path entirely, and verify a record absent from the mirror is reported as absent rather than fetched from the source
- [x] 4.3 Report a derivation running against an unreconciled mirror as such, and verify the derivation does not top up missing rows itself
- [x] 4.5 Preserve census containment correctness including interior holes, and verify agreement with the authoritative spatial answer on the same six addresses the original implementation was verified against
- [x] 4.6 Replace the fixed `-3` offset at `src/hotspots.py:37` with daylight-saving-aware conversion per timestamp, and verify two calls at the same local clock time in July and in January report the same local time — across a 2020–2026 range every record outside daylight saving is currently an hour out **[was 8.1]**
- [x] 4.6a Confirm the corrected conversion does not shift any doorway across the recency boundary, and verify by diffing the doorway list before and after. Calendar-date fields are expected to move where a timestamp lies within an hour of local midnight outside daylight saving — that is the correction, not a regression. Observed on 2026-09-16: membership and order unchanged (352 doorways), block list byte-identical, one `last_call` moved a day and 21 `median_gap_days` moved by half a day or a day **[was 8.3]**
- [x] 4.7 Carry the derivation time, last successful sync time and most recent call date onto every output, and verify all three appear together **[was 9.1]**
- [x] 4.8 Add automated coverage for the pure functions — address reduction, street extraction, point-in-polygon including interior holes, vehicle identity, recurrence counting — and verify the suite passes without network access **[was 12.4]**
- [x] 4.9 **Reconcile at figure level.** Run `src/hotspots.py` against the live service and the new derivation against the mirror for `Driveway`, and verify every row and figure in `out/watchlist.csv`, `out/blocks.csv` and both briefs matches — any discrepancy is a mirror or derivation defect, not a revision of the analysis. The cheap route is to point `query()` at the mirror and diff the outputs

### 4b. Compute the figures the documentation rests on

Carried from the archived change, and a prerequisite for 8.1: two figures central to the product's
claim exist only in hand-written prose and are reproduced by no generated output.

- [x] 4.10 Compute recurrence split by tow status and emit it, and verify the generated output reproduces the 44.7 per cent against 44.6 per cent comparison quoted in `README.md` **[was 11.1]**
- [x] 4.11 Report the effect size the comparison can rule out, and verify the output states a bound rather than only an absence of difference **[was 11.2]**
- [x] 4.12 State that the tow comparison is observational and that tows may cluster at the worst addresses, and verify the caveat travels with the figures **[was 11.3]**
- [x] 4.13 Compute median elapsed time from `DATE_INITIATED` to `DATE_CLOSED`, excluding and separately counting calls never closed, and verify the output reproduces the response-time figure opening `README.md` **[was 11.4]**
- [x] 4.14 Reconcile the call denominators in circulation — the DISPATCH label alone, the label plus `DRIVEWAY`, and the tow-comparison cohort — and verify each published figure names which population it is drawn from **[was 11.5]**

### 4c. Every canonical violation type

- [x] 4.15 Freeze the canonical violation-type list against a fresh live query of `Alleged Violation`, grouping each current label with its legacy short code and `(DISPATCH)` variant, excluding `Other` (7,542) and `Left Running`, and verify the frozen list accounts for the great majority of non-excluded rows — 71 distinct raw labels and 110,900 calls were observed on 2026-09-15 **[was 14.1]**
- [x] 4.16 Derive every tracked canonical type from one pass over the mirror, and verify no source query is issued and no doorway or block from one type appears in another's output **[was 14.2]**
- [x] 4.17 Compute recurrence, the tow comparison and vehicle uniqueness independently per canonical type, and verify a type whose figures do not resemble driveway's states its own conclusion rather than driveway's **[was 14.5]**
- [x] 4.18 Check `No Parking Sign` — 27,404 calls, the largest type — against the string-reduction concern in R2, and verify whether address collisions at that volume require coordinate keying ahead of the other types **[was 14.6]**
- [x] 4.19 Measure the all-types derivation time against the mirror, and verify it is bounded by computation rather than by network

## 5. Serve the application

- [x] 5.1 Serve the doorway list, block list and map over HTTP, and verify a viewer needs no install, account, credential or build step
- [x] 5.2 Route per canonical violation type scoping lists, ranking, markers and triage state, and verify switching types never mixes rows and that a view carries only its own type's data — `_canonical_for_slug` now resolves any slug in `_SLUG_TO_CANONICAL` instead of special-casing the default, which is the one-line lift 5.1 predicted; every list, marker and decision route was already written generically against `<slug>` and derives strictly one canonical type per request, so no code path returns more than one type's rows. Added `GET /types/<slug>` so a type has a real URL to land on or share, and `GET /api/types` for the plain `<select>` switcher the page builds from it. **Caught a real mixing defect in the process:** the page's `saveDecision()`/`loadDecisions()` still read the `DEFAULT_SLUG` constant, so every type's page would have written and read triage against `blocking-driveway` the moment other types became reachable — 6.1's tests could not see this because they exercised `mirror.triage` directly, where scoping was correct all along. Both now read the view's own `SLUG`, proved over HTTP. Verified against seeded disjoint data for two real types (addresses, blocks, coordinates and decisions each checked for non-overlap), plus all 30 frozen types answering. Timings track 5.5: 352–476ms default, 710–757ms for `No Parking Sign`; `/api/types` needs no database
- [x] 5.3 Report an untracked type as untracked, and verify a route naming one renders a stated reason rather than an empty list — as of 5.2 both `/types/<slug>` and the JSON routes answer an unknown slug with a bare 404 (Flask's default HTML page for the former), which is what this task replaces with a rendered explanation
- [x] 5.4 Serve the street network as a cacheable asset, and verify moving between types does not re-download the 490 KB geometry — `/map-network.json` now sends `Cache-Control: public, max-age=86400` and an ETag, so a browser that holds it skips the request entirely for a day rather than re-asking on every type switch, and a request past that window gets a 304 with no body. Not `immutable`/a year: with no build step there is no cache-busting filename (`design.md` M12), so a re-derived network needs a bounded staleness window. Verified live by `curl` and by two tests in `tests/test_app.py`, one of which round-trips a real 304
- [x] 5.5 Compute derived results per request against an indexed mirror rather than materializing per filter combination (`design.md` M11), and verify a view's response time under the default parameters and under a widened threshold — **no index was missing.** Every query the per-request derivation issues was audited with `EXPLAIN (ANALYZE, BUFFERS)` against the fully loaded mirror rather than assumed, and all three load-bearing indexes already existed from 1.5; `schema.sql` now records per index which read uses it and what it measured, and no index was added, dropped or altered. Measured warm on the live mirror: `/api/types/blocking-driveway/doorways` 349ms median, `/blocks` 334ms. Widening does not degrade it — `min_calls=1` returns 3.8x the doorways (1,340 vs 351) in 294ms against 298ms at the default, and a three-year recurrence window runs 287ms. The largest type, `No Parking Sign` at ~3x Driveway's volume, is 662ms default and 646ms widened. Profiling puts the 0.3–0.5s roughly half in index-backed SQL round-trips and half in Python already known to `hotspots.build()`/`find_block()` (census ray-casting), so the ceiling is computation on an indexed result set, not a missing index — M11's premise holds and no materialization is warranted. **Caveat:** the widened-threshold figures were taken by calling `derive.derive()` with widened arguments, because no HTTP surface exposes filter parameters until 5.5a; that is the same code path a future control will trigger, but it is not a live widened request
- [x] 5.5a Provide district, minimum recent calls, minimum still-calling doorways, recency window and recurrence window as interactive controls, and verify results and ranking update correctly as each changes. **Checked 2026-09-17:** verified over real HTTP requests (`tests/test_app.py`), not just by calling `derive_with_recency` directly — closes 5.5's own note that its timings weren't from live requests. No browser exists in the agent environment, so "watch it update in a browser" is unproven the same way 5.6 was; ~30 seconds of a human clicking Apply filters would close this out
- [x] 5.5b Expose the recency window, currently hardcoded at `src/hotspots.py:214` (`recent_from = latest - 365 days`), as a parameter, and verify changing it changes which doorways are listed and the ranking **[was 10.1]**. Implemented as a new module (`src/mirror/derive_recency.py`) that shifts the `latest` value handed to `hotspots.build()` rather than editing `hotspots.py`/`derive.py`, which stay untouched for 4.9. A first attempt at this exact approach had an arithmetic bug that broke `calls_12mo` window membership at the boundary; this attempt tests the boundary directly (a call exactly `recency_days` old vs. one day older) rather than trusting the algebra, and reproduces `derive.derive()`'s output field-for-field at the default of 365 days
- [x] 5.5c Present the recency and recurrence windows as two distinctly labelled controls, and verify that adjusting the recency window changes the listed set while adjusting the recurrence window changes only the repeat figure — they share a default of 365 days and govern different things. **Checked 2026-09-18:** labels carry a `title` hover hint stating what each alone governs (same convention 7.6 used, same touch/keyboard caveat applies); the behavioral claim is proven over real HTTP (two seeded doorways, narrowing recency drops one from the list, narrowing recurrence only changes its `repeat_calls` figure). Required exposing `repeat_calls` (`"rp"`) in the doorway JSON payload, which nothing in the UI displays yet — worth a follow-up if that figure should be shown, not just provable
- [x] 5.5d State the filter values in effect on every view and export, and verify a filtered list cannot be mistaken for the default list. **Checked 2026-09-18:** a non-empty, non-default view now states which filters differ from default (distinct from 7.4's unconditional recency note and 5.5e's empty-result naming), backed by a test pinning the client-side default literals against `server.py`'s `_FILTER_DEFAULTS` so the two can't drift. The "export" half is not yet buildable — no export mechanism exists until 7.7 — but `activeFilterBits()`/`activeFilterSummary()` and the plain `FILTERS`/`FILTER_DEFAULTS` data are there for 7.7 to reuse directly
- [x] 5.5e Report an empty filter result as empty and name the values that excluded everything, and verify it is distinguishable from a failure. **Checked 2026-09-18:** the server always answers 200 with empty `rows`/`blocks` plus the `filters` actually applied (never a 4xx/5xx); the page names those filter values in both the results count and the empty-list message, distinct from the two genuine-failure banners and from the pre-existing client-side-only "no matches" wording
- [x] 5.6 Preserve light and dark rendering and following a preference change while open, and verify by toggling the system colour scheme with the application open — **found and fixed a real defect:** the canvas map's triage-status marker and block-fill colours were a fixed hex table carried over from `web/template.html`, so in dark mode a marker disagreed visibly with the legend swatch beside it (the legend reads `--hazard`/`--alert`/`--accent`/`--line-2`; the markers did not). `draw()` now builds those four colours from the same tokens on every call, and the existing `matchMedia` change listener repaints, so markers follow a live scheme change like the rest of the page. **Verification is code-level, not observed:** no browser is available in this environment, so the literal toggle-while-open check the task asks for has not been performed. What was proven by reading is that every colour the page paints flows either through CSS custom properties (re-evaluated by the browser on a scheme change, by construction) or through `draw()`'s per-call `CSSVAR()` reads, and that the light and dark blocks define matching token sets. A human should still toggle the scheme once against the running application
- [x] 5.7 Compute and store each canonical type's filter-independent figures — per-type conclusions with their intervals, the tow effect bound and the call denominators — after every reload, keyed to the mirror version they were derived from, with a command that computes them for the version currently held, and verify a reload stores figures for every type and a second run against the same version does not duplicate them (`design.md` M12)
- [x] 5.8 Serve filter-independent figures from what 5.7 stored rather than computing them per request, and verify a view reports figures as unavailable, rather than computing them, when none are stored for the mirror's current version. This includes the tow comparison hard-coded into the markup at `web/app/index.html:280` — now wrapped in `<span id="tow-thesis">` and blanked to "not yet available" for any non-default type by a stopgap 5.2 added to stop Driveway's figure displaying as another type's; **that guard is 5.8's to remove**, along with the frozen number itself — "A tow does not lower the chance a doorway calls again: **44.7 per cent against 44.6 per cent**" — which must be filled from the stored figures like every other figure on the page, and verify the sentence carries the current type's stored comparison rather than a number frozen into the file. **Checked 2026-09-17:** new `GET /api/types/<slug>/figures` route wraps 5.7's `mirror.type_figures.figures_for`; the served sentence now substitutes the current type's stored two numbers into the same fixed template rather than varying the qualitative claim by the stored `tow.conclusion_key` (tow_lower/tow_higher/no_difference/sample_too_small) — a deliberate reading of "filled from the stored figures" as the numbers only, but worth a second look, since for a type whose bootstrap concludes something other than "no difference" the fixed template's framing may not match its own stored conclusion

## 6. Shared triage and role

- [x] 6.1 Persist a decision and its note server-side, and verify a decision recorded by one viewer is visible to a second viewer of the same type's list — `src/mirror/triage.py` reads and writes the `triage_decisions` table 1.5 defined, upserting on its `(violation_type, scope, item_key)` constraint, and `src/app/server.py` exposes it as GET and POST `/api/types/<slug>/decisions`. The page now loads every decision for its type on boot and POSTs each change, in place of the `window.claude.use("db")` capability that resolved to `null` off a Claude Artifact and degraded every viewer to a private `localStorage` copy. Verified by two independent `create_app()` instances sharing only the database: one records, the other reads it back, note and all — and separately end-to-end through three `curl` processes against a running server on real Postgres. Type and scope separation is enforced twice, in `list_for`'s WHERE clause and in the table's UNIQUE constraint. `role` is written as the single placeholder `PLACEHOLDER_ROLE` for 6.6 to replace. **Note for 6.3:** the dead `loadLocal`/`saveLocal`/`useCap` paths are kept as that task requires, but the null-`db` branch no longer calls `loadLocal()` — left unconditional it would have overwritten the server-backed decisions with an empty per-browser copy on every load, reinstating the exact defect this task closes
- [x] 6.2 Verify decisions survive an application restart. **Checked 2026-09-17:** proven with two independent `create_app()`s against the same database — one writes a decision, the second (standing in for a process restart) reads it back
- [x] 6.3 Remove the browser-storage fallback and the `window.claude.use("db")` binding from the served page — now `web/app/index.html` (the `localStorage` read and write at 455-458, the `useCap` binding at 1056) — and verify no code path leaves a viewer believing a local-only decision was shared. **Checked 2026-09-17:** 5.1 evolved `web/app/index.html` as a new file rather than feeding `web/template.html` from the API as `design.md` M12 anticipated, so both files carry both strings. The served page is the one this task edits. `web/template.html` keeps its copy of both: per `design.md` M5 a deprecated file's defects are out of scope to correct, and the file itself goes with 8.7. `loadLocal`/`saveLocal`/`LS_KEY` and the `window.claude.use("db")`/`onSnapshot` block are removed; the `useCap()` helper function itself is kept, since it also backs the unrelated "Ask Claude" advice sampler — removing it would have silently broken that feature, which nothing in this task's text asks for
- [x] 6.4 State plainly when a decision could not be persisted, and verify an unsaved decision is not displayed as recorded. **Checked 2026-09-17:** `save()` now keeps the pre-write row and rolls back to it (re-rendering) if the POST fails, rather than leaving the optimistic write standing — this is the fix for the failure mode the handoff flagged ("a failed triage write may still display as recorded")
- [x] 6.5 Attribute each decision to a role and a last-changed time, and verify both are visible on a triaged doorway and block
- [x] 6.6 Ask for a role before a decision is recorded while leaving the lists readable without one, and verify both paths. **Checked 2026-09-17:** role is asked once (cached client-side thereafter) the first time a decision is recorded, not at page load — browsing, filtering, search, map and export need no role
- [x] 6.7 State that roles identify rather than authenticate, and verify the application makes no access-control claim and grants no differing access by role

## 7. Freshness, limits and export in the served views

- [x] 7.1 Display a prominent message on every view giving the last successful update and the expected next update, and verify it is visible without being sought **[was 9.2]**. **Checked 2026-09-17:** served by new `GET /api/freshness`, a thin wrapper over 3.3/3.4's existing clocks; a first attempt at this route compared raw `.isoformat()` strings across timezone-aware datetimes and failed three tests on offset representation alone (`-03:00` vs `+00:00` for the same instant, since psycopg returns `timestamptz` values in the connection's session timezone) — this attempt normalizes every timestamp through `_iso_utc` before serializing and comparing
- [x] 7.2 Attribute a lag to whichever system owns it, and verify that a source that has not published for days reads as a limit of HRM's publishing schedule while a sync that has fallen behind a published source reads as this system being behind. **Checked 2026-09-18:** reuses `mirror.status.layer_freshness`'s existing per-layer `behind_reason` wording (not recomputed); both directions proven with separate fixtures (healthy sync against a quiet source vs. a sync that has itself fallen behind a fresh publish) rather than trusting the wording reads correctly
- [x] 7.2a Make the next update a state rather than a rendered date, and verify that once the scheduled time has passed by the grace period with no successful sync the message reads as overdue rather than continuing to show a future date that never arrives. **Checked 2026-09-18:** grace period grounded in `mirror.sync.POLL_INTERVAL` (this sync's own polling cadence, one day — distinct from 7.4a's 21-day HRM-publish-cadence threshold), not picked independently. Boundary verified directly against `_next_update_state` (exactly at the grace deadline vs. one second past; a success recorded exactly at vs. one second before the due time)
- [x] 7.2b State that the next source update is not yet known where too little history exists, and verify no estimate is fabricated from a single observation
- [x] 7.3 Remove the undated "Regenerated from the live service, not from a stored export" assertion from the served page (`web/app/index.html:379`), and verify the view shows dated freshness rather than a claim — the sentence is also now false, since it is served from a mirror. **Checked 2026-09-17:** the same sentence survives at `web/template.html:379` and in the generated `out/triage-board.html`; per `design.md` M5 both are deprecated and left as they are, so this task changes the served page only **[was 9.3]**
- [x] 7.4 State the recency window's anchor date and length on every filtered list, and verify the wording names the date the window is measured from **[was 10.3]**. **Checked 2026-09-18:** stated unconditionally (the default 365-day window is itself a window), anchored to the mirror's true latest call date (not the internal shifted value `derive_recency` feeds `hotspots.build()`)
- [x] 7.4a Warn when the most recent call in the data is materially older than the run time, and verify the warning fires against a deliberately stale input **[was 10.2]**. **Checked 2026-09-17:** threshold set to 21 days (design.md M3's roughly-weekly observed cadence, times three, so it doesn't fire on the ordinary gap between two publishes); verified against a deliberately stale fixture, per the task's own wording, not just unit-level
- [x] 7.5 Carry the interpretation limits into the served views — intake clock, tow as the only recorded outcome, vehicle identity as a floor, text address reduction, derived block labels, block-wide dwelling rate — and verify each is reachable from the view carrying the figures it bears on **[was 12.2, 12.3]**. **Checked 2026-09-18:** each limit is a visible, focusable footnote link (not a bare hover `title`, rejected here per 7.6's own noted weakness) from the header stats/thesis figures to a matching entry in a restructured "Read these numbers carefully" footer list. Also varied the tow-thesis sentence's qualitative claim by the stored `tow.conclusion_key` (previously a fixed "does not lower the chance" template with only the two numbers substituted — 5.8's own note flagged this as possibly wrong for a type whose stored conclusion says otherwise), read as in this task's scope since an unsupported qualitative claim is itself an interpretation-limit violation. **Known gap:** the dwelling-rate and derived-block-label footnotes could only be anchored to the header stats, not to their actual figures in the results list/detail panel (`evidenceBlock()`), which is a different region owned by a concurrent agent this pool — a follow-up task should add the same `#lim-dwelling`/`#lim-block-label` links there for full reachability **Follow-up (landed with 7.7a/7.8):** the `[block label]` and `[dwelling rate]` limit links now also appear in the detail panel (`evidenceBlock()`). List rows are `<button>`s, so a nested link would be invalid HTML; a `#list-limits` line above the block list carries them for the rows instead. Not checked in a browser.
- [x] 7.6 Explain how to read the block neighbour count before ordering a remedy, and verify a doorway with several still-calling neighbours is presented as a block-level problem — most of this was already true and inherited from `web/template.html` via 5.1: `evidenceDoor()` renders "N doorways on this block are still calling, so a sign here alone probably will not settle it. Open the block view." directly above the remedy buttons in the detail panel, and the orange `.flag` styling marks any row with more than one still-calling doorway at a glance. The gap closed here is the collapsed row, where the "On block" count had no explanation at all: the column header and each row's value now carry `title` text saying what the number counts and that more than one means a fix aimed at the single address is the wrong scope
- [x] 7.7 Export a type's doorway and block lists as a complete table and a readable brief, and verify the export carries every listed row, the violation type it covers, the clocks, the filter values and the same limits as the view **Note:** the routes were not reachable from the page until the follow-up that added the `#list-exports` line (CSV download and readable brief, carrying the filters currently applied). Not checked in a browser.
- [x] 7.7a Name the canonical type each export and view covers, and verify a reader can tell which type they are looking at without cross-referencing a filename **[was 14.7]** **Note:** the served page's eyebrow had hard-coded "blocked driveway service requests" on all 30 type pages; it, the list heading and the document title now come from the payload's type. CSV rows carry a "Violation Type" column, brief tables carry captions, and the untracked-type page states it covers no type. Tests read content only, never a filename or URL.
- [x] 7.8 Verify a view and an export taken together agree on every count and name the same mirror state **Note:** the tests found a real disagreement, now fixed: the brief printed the word "None" for missing values while the view and CSV showed null/blank. **Caveat, since fixed for the two export routes:** an export used to read its rows and its freshness clocks in separate queries, so a sync landing mid-request could name a different mirror state than its rows. Both routes now run in one `REPEATABLE READ READ ONLY` transaction (`_pin_snapshot`), proven by a constructed interleaving test that fails without the fix. **Still open, same shape:** `GET /api/freshness`, and the derivation's own separate reads on `/doorways` and `/blocks`, are not pinned.

## 8. Reconcile the documentation with generated output

The repository currently states 9,791 calls, 363 doorways and 71 blocks in `README.md` and under
`docs/`. A run on 2026-09-15 produced 9,834, 351 and 70. The figures were hand-written and have
drifted within days. A public URL turns that into a credibility problem. Section 4b must land
first, or the figures 8.1 needs still will not exist.

- [x] 8.1 Replace every hand-written figure in `README.md` and `docs/parking-hotspots/` with the current generated value, and verify each published number is reproduced by output the reader can regenerate — **completed in part; the remainder is deferred to a future change.** Done: every figure in `README.md`'s "What it found", "Blocks, not doorways" and handout sections, and in `docs/parking-hotspots/product.md` and `data-sources.md`, re-read from the current mirror and annotated with the command that regenerates it. Not done, deliberately: (a) the `README.md` sentences that 8.4 rewrites wholesale were left alone rather than corrected twice; (b) several one-off counts (the `RESOLUTION` and `COMMUNITY` shares, the live-layer year counts) are annotated with the module they were counted from but have no regeneration script, so "reproduced by output the reader can regenerate" holds only for the figures with a CLI behind them; (c) `docs/parking-hotspots/decisions.md` keeps its original figures on purpose — it is the narrative record of what was decided and on what evidence, and restating it against a later mirror would falsify that record. Writing the missing scripts, and settling how a dated decision record cites figures that have since moved, are a future change's work **[was 13.1]**
- [x] 8.1a Re-derive the hour-of-day and channel figures in `docs/` under the corrected timezone conversion, and verify the published numbers match the corrected output or are updated to it **[was 8.2]**
- [x] 8.2 Correct `README.md`'s hosted-copy claim — "The hosted copy shares those decisions with everyone who opens it, so a team triages one list instead of three" — to describe the served application, and verify it no longer describes a Claude Artifact. **Checked 2026-09-18:** now reads "The served application persists those decisions in the mirror's own database, so anyone who visits its URL sees the same triage state, and a team works one shared list instead of three"; grep confirms no remaining "Claude Artifact"/"hosted copy"/`window.claude.use` language in `README.md`. **Note:** the "Open the board" paragraph immediately above (describing `out/triage-board.html`'s double-click/browser-local-storage behaviour, left uncorrected per M5) now sits awkwardly next to this corrected sentence with no transition — task 8.4's rewrite of that paragraph should resolve the sequencing gap
- [x] 8.3 Qualify `README.md`'s "No keys and no install" to the viewer rather than the system, and verify it does not imply the server has no dependencies — the claim now names the viewer as its subject in its own sentence, and the server's dependencies are named concretely (Postgres, Flask behind a WSGI server such as gunicorn) rather than left as "do have dependencies". The neighbouring "No server, no install, no account" in the "Open the board" section is 8.4's, not this task's
- [x] 8.4 Replace `README.md`'s "Open the board" instructions to double-click `out/triage-board.html` with the URL and the export path, and verify a reader is not directed to a file that is no longer the product — including `docs/index.md`'s file table, which today lists `out/triage-board.html` as the board and names `web/template.html` as its source. Both are deprecated by this change (`design.md` M5); the correction owed is in the documents pointing at them, not in the files themselves **Note:** `README.md` and `docs/index.md` now point at the URL and the two export routes. `docs/parking-hotspots/product.md` (about lines 72-79 and 95) still tells readers to run a bare `python3 src/hotspots.py`, which now needs output paths; that is left to 8.9.
- [x] 8.5 Record in `docs/parking-hotspots/decisions.md` the decisions this change makes — mirror, source-paced sync, served application, shared triage, role selection, and the removal of this repository's scheduled job — with their reasons, and verify the narrative record matches these specs — done: a new "Decided under `mirror-hrm-data-and-host-app`" section in `decisions.md` (before "Open for Chris"), additions only, ten entries from design M1–M12 (the six named plus staleness as four clocks, the reload-on-publish reversal, the M12 outcome with the `b04731c` pointer, and the analysis carried over unchanged). Figures were spot-checked against `design.md`/`proposal.md`. **Caveats, found while cross-checking and left unresolved:** `proposal.md` still describes the retired watermark/open-set sync and says `out/` becomes "at most an export"; the spec, design M4 and 3.1 still list a "watermark" as a recorded sync outcome; design leaves the grace period open while 7.2a set it from `POLL_INTERVAL`; the page's `ROLE_OPTIONS` has a third role, "Other", that the spec and design do not name
- [x] 8.6 Retain the doorway and block lists per sync in the store, and verify the record of which doorway left the list and when survives the removal of the scheduled job that was building it by accident in git history
- [x] 8.7 Stop generating committed output to `out/`, and verify the exports in 7.7 are the only way a list leaves the application **Note (M12 resolved by the user):** generation removed from `src/hotspots.py`; `web/template.html` and the five tracked `out/` files deleted, with the last commit containing them (`b04731c`) recorded in `README.md` and `docs/index.md`; the `--board` flag removed from both `hotspots.py` and `reconcile_figures.run_live`. **Caveat:** "the only way a list leaves the application" is read as the served application. The operator scripts `src/mirror/derive.py` and `src/hotspots.py` (the 4.9/9.4 live baseline) still write lists to a caller-named directory, and `/api/types/<slug>/doorways` and `/blocks` return lists to the page.
- [x] 8.8 Correct the unsourced "weekly-refreshed source" claim wherever it is repeated, and verify no document states a source refresh interval that nothing measured supports
- [x] 8.9 Sweep stale references to the retired `web/template.html`, the deleted `out/` files and the bare `python3 src/hotspots.py` command from code comments, docstrings and documents, and verify none is described as current (a past-tense or `b04731c` pointer is fine). Known sites, line numbers approximate: `src/app/server.py` docstring (top, about lines 4-15) and the `write_board` mentions near lines 411-461; `web/app/index.html` comments naming the template or `write_board`; `tests/test_app.py` comments naming `write_board`; `src/mirror/reconcile_figures.py` about lines 48-51, 89-96 (`BOARD_STATUS` says hotspots.py "still writes a board") and 110-111; `src/mirror/derive.py` line 5 ("hotspots.py is untouched"); `src/mirror/triage.py` line 7; `docs/parking-hotspots/product.md` about lines 72-79 and 95; `src/mirror/history.py` line 6, `src/mirror/schema.sql` line 4 and `tests/test_history.py` about lines 268 and 289 (check whether these read as past tense before changing). Do not touch `openspec/` archive files — done in the served application, `mirror/` modules, `reconcile_figures`'s `BOARD_STATUS` (descriptive only; no logic changed) and `product.md`'s "Run it"/"Real"/"Stubbed" sections; the final grep leaves every remaining hit past-tense, a `b04731c` pointer, or legitimately current (`hotspots.py` is still the 4.9 baseline with four required output flags). **Caveat:** `product.md` line ~46 ("A scheduled job that reads HRM open data and writes a ranked list") and line ~99 ("All run live against HRM open data") were outside the sweep's search terms and are still stale against the mirror and the removed scheduled job; left for the user's decision because they are product wording

## 9. Verification

- [x] 9.1 Verify the application serves correctly with the HRM service unreachable, and that its views state the mirror's freshness **Checked 2026-09-18:** every route (11, enumerated from the URL map, for every tracked slug, plus the decisions POST) answers from the mirror alone with the source patched to record and refuse any call; a real failed sync (`URLError`) is recorded with its error, changes nothing served and does not advance last success; the API, both exports and the page banner name the last success and the most recent call. **Caveat:** the page banner shows only the last success, not the failed last attempt (the API and CSV carry both). Unreachability is simulated; nothing ran with HRM actually down, and the banner was exercised in node with stubs, not a browser. Tests: `tests/test_verify_unreachable_and_overdue.py`
- [x] 9.2 Verify a sync stopped past its scheduled time produces a visibly overdue application rather than a healthy-looking one displaying a future update that never arrives **Checked 2026-09-18, NOT closed: four defects found, each held as a strict xfail in `tests/test_verify_unreachable_and_overdue.py` (remove the marker when fixed).** The core property holds: overdue is `now > due + 1 day` (strict; `NEXT_UPDATE_GRACE_PERIOD = POLL_INTERVAL`), tested at, before and after the boundary with stored offsets of UTC, -03:00 and +05:30, and an overdue sync shows no upcoming date. Defects: (A) inside the grace window the banner reads "Next update expected <past time>"; (B) the overdue banner never says how long it has been overdue, though the spec says it names it and the API carries `days_overdue`; (C) a passed estimate of HRM's next publish is shown as "estimated to next publish around <past date>"; (D) a stopped sync with an old newest call reads overdue yet the banner says "this sync is caught up with HRM ... not a problem with this mirror", and the API's `behind_reason` blames HRM's schedule, which is the design M4 whose-limit misstatement **Progress 2026-09-18 (Pool 11, cut short by a usage limit):** defects A and B are FIXED (`_next_update_state` now returns `due_passed` and `seconds_overdue`; the grace-window banner reads "The next sync was due X and has not completed yet.", the overdue banner reads "Next update is overdue by N days (counted after a 1-day grace period)", hours under a day, floored) and their xfail markers are removed; **C and D are NOT fixed** (their 3 xfail markers remain) and 9.2 stays open until they are **Closed 2026-09-18 (Pool 12):** C and D are FIXED and all four defects' xfail markers are removed. C: `_next_source_estimate` returns `passed` (`now > estimated_next`, strict: an estimate exactly equal to now is still upcoming) and a passed estimate reads "HRM was estimated to publish around X, but this mirror has not recorded a newer publish". D: `_behind_reason` takes `sync_overdue`, so an overdue sync with no layer behind says "this sync is overdue, so this mirror cannot tell whether HRM has published since ... the lag cannot be put down to HRM's publishing schedule until a sync completes", and the banner's stale-data branch says the same instead of "caught up with HRM"; a layer that is behind still reports HRM's publish. Boundary-case tests were added for each, and each fix (and B's) was proven by reverting it in a scratch copy of the tree, where the named tests fail. Verified in node against the page's own script, not in a real browser (concern 1).
- [x] 9.3 Verify no 311 call-detail value is read, referenced or implied anywhere in the delivered system, distinguishing it from the `311 Online` value of `INITIATED_BY` on the service requests layer, which is a channel label and is legitimately used **Checked 2026-09-18:** a repository-wide audit found 0 references that read, join, name or imply 311 call-detail data (13 `311 Online` channel-label hits, none in `src/` or `web/`; 10 hits that record the exclusion); the mirror's layer list is the three services only, a full sync requests only those three, the schema has no 311 table or column, and no served surface (page, JSON routes, both exports) names or implies 311 call details. **Caveat:** the audit test scans `tests/` and `docs/`, so a later document or test that mentions 311 call details outside the four exclusion-record files will fail it. Tests: `tests/test_verify_no_311_and_claims.py`
- [ ] 9.4 Verify every figure served for `Driveway` matches the pre-change live-source run, per 4.9, after the application is serving from derived tables **Progress 2026-09-18 (Pool 11, cut short by a usage limit; NOT closed):** the freshness gate passed (mirror equals HRM's publish for both layers) and one live run of `reconcile_figures.py --violation Driveway` reconciled 351 doorways and 70 blocks with all four files identical (derive matches live: 4.9 re-proven on today's data; outputs kept under `orchestration-progress/worker-J-artifacts/`). **Not done:** the served-vs-live comparison (`compare_served_vs_live.py` written, never run) and the mirror-proof of the offline test `tests/test_verify_served_matches_derive.py` (4 pass, 4 held as strict xfail because of a test bug: served JSON rows carry an `i` rank key). **One possible discrepancy, unverified:** `/api/types/blocking-driveway/figures` says "96% of 2528 seen" where the live brief says 95 per cent unique, attributed to double rounding at `src/mirror/per_type.py:285`; if confirmed, this is a served figure that does not match the live run **Progress 2026-09-18 (Pool 12; NOT ticked, one decision left for the user):** the freshness gate passed again (mirror still equals HRM's publish for both layers) so no second live run was needed, and the kept live outputs were compared with everything served, GET-only on the real mirror: JSON `/doorways` (351 rows x 17 fields) and `/blocks` (70 x 11), `export.csv` and the per-type export tables, both live briefs' tables and header figures, all with 0 mismatches (`orchestration-progress/worker-J-artifacts/served-vs-live-report.txt` lists every normalisation; the comparison was shown to catch a tampered cell). The offline test `tests/test_verify_served_matches_derive.py` is finished (8 pass, mutation-proven in a scratch tree: dropped row, changed count, UTC date). **The one mismatch, concern 24, is confirmed and FIXED IN CODE:** `per_type.py` rounded 2413/2528 to 0.955 then to "96%"; the live brief and `/doorways` say 95; `_unique_pct_text` now rounds once (`tests/test_verify_vehicle_pct_rounding.py`, 12 tests, fails with the fix reverted). **Still open:** `/figures` serves a stored `type_figures` row (Driveway, computed 2026-09-16) that still says 96% until that row is recomputed or the mirror version changes, so the running application still differs from the live run on that one sentence; recomputing writes to the real mirror, so it is the user's call. `README.md:94` and `docs/parking-hotspots/product.md:36` said "96 per cent unique" for 2,413/2,528 and now say 95 (edited 2026-09-18 at the user's direction, as the two documents that describe the app going forward). `decisions.md:28` (a historical record with its original figures) and `five-problems-research.md:172` (dated 2026-09-12, different data: 2,473/2,588 = 95.56%, so its 96 is correct) were left as they are.
- [x] 9.5 Verify the board and the exports from one mirror state agree on every count, including the provenance and comparison figures added in 4b **[was 13.2]** **Checked 2026-09-18:** every count in the view is re-derived from a seeded ledger in plain Python and compared with the view JSON, the CSV, the brief and the page's own JS (node, stubs) for five types (conclusive, no-difference, too small, tracked-no-calls, figures-never-stored) plus an untracked slug, default and non-default filters including a recurrence-window boundary and calls near Halifax midnight (ADT and AST); all agree, and a GET-only comparison on the real mirror for Blocking Driveway (4 filter sets) also agrees. **Caveats:** the header's "ended in a tow" and "of vehicles unique" percentages, and the vehicle and neighbour sentences, appear in neither export (the tow percentage's denominator is every call of the type, so no export's rows reconstruct it); the 4b provenance figures (`call_denominators`, `response_time`, `tow.effect_bound`, `population`) are served only by `/figures`, shown by neither the page nor the exports, so their agreement was checked against the ledger and not between view and export; the mirror was quiet, so the unpinned `/api/freshness` and `/doorways` reads were not exercised. Tests: `tests/test_verify_view_export_agreement.py`
- [x] 9.6 Verify two viewers in different roles triage one shared list and each sees the other's decisions with role attribution **Checked 2026-09-18:** two independent apps over one database, a coordinator and an officer, each see the other's decision, note, role and changed-time on both a doorway and a block; attribution follows the last writer; type X's decisions are not returned for type Y; reading needs no role; no role gets different access; a failed write returns 500 and records nothing, and the page rolls back and says it was not recorded. **Caveats:** the server accepts any role string verbatim (a missing role is stored as `unspecified`), so "coordinator or parking enforcement officer" and "asked to choose a role" are enforced only in the page, which also offers "Other"; the page says a role "does not log you in" but never says anyone with the URL can record a decision; decisions load once at boot with no polling; **one defect, FIXED 2026-09-18 (Pool 12):** `updated_at = now()` was the start of the request's transaction, so a later writer whose connection opened before an earlier writer's whole request could get an earlier changed-time; `triage.record()` now stamps `clock_timestamp()` on both the INSERT and the DO UPDATE path (`test_the_last_writers_changed_time_is_later_even_if_its_connection_opened_first` plus a new INSERT-path test; both fail with `now()` restored). Other `now()` writers (`load.py`, `history.py`, `type_figures.py`, `schema.sql` defaults) are single-writer bookkeeping and were left alone. Tests: `tests/test_verify_shared_triage.py`
- [x] 9.7 Verify no served view or export claims an enforcement outcome from the `RESOLUTION` field or an hour-of-day finding from pooled timestamps **[was 13.4]** **Checked 2026-09-18:** `RESOLUTION` is stored and carried but never selected, grouped or surfaced by any served page, JSON route or export (seeded enforcement-sounding values appear nowhere, and changing them changes no output); the tow flag is the only outcome any view states and it is framed as the only recorded outcome; no served text, payload key, CSV column or page code makes or extracts an hour-of-day claim (calls near local midnight in both ADT and AST carry calendar dates only). No defect. **Not verified:** runtime rendering in a browser. Tests: `tests/test_verify_no_311_and_claims.py`
- [x] 9.8 Verify no tracked violation type presents blocked driveway's conclusion without its own effectiveness evidence supporting it — D19's requirement survives the archiving of the change that stated it **[was 14.3, 14.5]** **Precondition (deferred to this section's dispatch):** the "Why blocks" footer hard-codes driveway numbers on every type's page, and the Ask-Claude prompt says "blocked-driveway complaints" for every type. Make both type-specific or remove them before running this check. **Done 2026-09-18, precondition met:** the "Why blocks" footer text was removed (its driveway figures had no per-type source; the six `lim-*` limits and "The map" are kept and every `#lim-` link resolves), and the Ask-Claude prompt is built by `buildAskPrompt` from the payload's type name, keeping the enforcement sentence only when that type's own `figures.tow.conclusion_key` is `no_difference` and dropping the remedy list. The audit also found two header sentences that stated driveway's conclusions on every type ("no repeat offender to deter, so the street is producing the violation"; "most of these are not a sign at one address"); they are now chosen from the type's own `vehicles.conclusion_key` and its own neighbour count. Verified with three seeded types (`no_difference`/`mostly_distinct`, `tow_higher`/`substantial_repeat`, `sample_too_small`) over the served page's own JS in node, and each guard was shown to fail against the old page and against mutants. **Caveats:** two remedy-framing sentences remain on every type ("a sign here alone probably will not settle it" and "Putting a sign at each of the N addresses is N interventions for one problem"), being block-versus-doorway framing and not tow or vehicle conclusions; the tow sentence is also absent in the short window before `/figures` loads; nothing was rendered in a browser. Tests: `tests/test_type_conclusions.py`
