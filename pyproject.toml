[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
markers = [
    "db: needs the Postgres mirror; errors, never skips, when it is unconfigured or cannot be reached",
]
