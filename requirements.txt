# Server-side dependencies. This project previously had no manifest at all: the
# pipeline in src/hotspots.py is stdlib-only and stays that way, so the viewer
# still needs no install. The mirror and its tests do.
#
#   python3 -m venv venv
#   venv/bin/pip install -r requirements.txt
#   venv/bin/pytest

psycopg[binary]==3.3.5
pytest==9.1.1

# Loads the local .env (database connection; see .env.example) into the environment.
python-dotenv==1.2.3

# Task 5.1: the served application (design.md M12). Flask is synchronous and
# small; a production WSGI server such as gunicorn runs it. src/hotspots.py
# stays stdlib-only -- these are the server's dependencies, not the viewer's.
flask==3.1.3
gunicorn==26.2.0
